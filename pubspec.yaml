name: toeicmobileapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  provider: ^6.1.5+1
  flutter_tts: ^4.2.2

  flutter_dotenv: ^6.0.1

  record: ^6.0.0  # Downgrade từ 5.1.0 xuống 4.4.4
  audioplayers: ^5.2.1
  path_provider: ^2.1.2

  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  google_sign_in: ^6.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/cassette_image.jpg
    - .env